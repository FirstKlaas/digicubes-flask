{% macro row(form, field_name) -%}
    <div class="row">
        {{ form[field_name](grid="s12") }}
    </div>
{%- endmacro %}

{% macro submit(form) -%}
    <div class="row">
        <div class="col s12 right-align">{{ form.submit }}</div>
    </div>
{%- endmacro %}
